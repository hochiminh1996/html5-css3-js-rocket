// declara uma variável de nome weight
let weight;

// Qual é o tipo de variável acima ? Undefined

// Declare uma variável e atribua valores para cada um dos dados
let nome = "Felippe"; //string
let idade = 25;//number
let divisao = 12.5; //number float
let humano = true;//boolean

// a variável abaixo é de qual tipo?
let studant = {}; //object. Um objeto vazio

// declare uma variavel array de nome students e atribua a ela nenhum valor, ou seja, um array vazio
let studants = [];

//insera o objecto studant dentro doarray studants

studants.push(studant);


// cria um novo objeto e adiciona ao studants.
const felippe = {
    nome: "Felippe",
    idade: 25,
    curso: "Análise e Desenvolvimento de Sistemas"
}

studants.push(felippe);



